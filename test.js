// Example utility - not a Jest test file
// write a function that adds two numbers together and returns the result

function add(a, b) {
  return a + b;
}

// Export for potential use
module.exports = { add };
